from datetime import date
from urlSchema import crawl as urlSchema
class startCrawl:
    def crawlerDetails(self):
        self.crawlerName= "B5-01"
        self.organisation = "Dravel Search"
        self.crawlerDescription = "This is a crawler for the B5-01"
        self.author = "Dravel Search"
        self.crawlerVersion = "1.0"
        self.crawlerDate =date.today()
        return self
urlSchema(self) 
        
        
        
    
        